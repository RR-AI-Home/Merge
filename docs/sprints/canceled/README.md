# Canceled Sprints

Canceled or superseded sprint records go here as source/reference material only.

If an idea becomes active again, write a new sprint doc under `../active/` instead of reviving old instructions wholesale.

